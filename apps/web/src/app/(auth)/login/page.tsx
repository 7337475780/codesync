import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login - CodeSync",
  description: "Sign in to your CodeSync workspace",
};

export default function LoginPage() {
  return <LoginForm />;
}
