import { SignupForm } from "@/components/auth/signup-form";

export const metadata = {
  title: "Sign Up - CodeSync",
  description: "Create your CodeSync workspace",
};

export default function SignupPage() {
  return <SignupForm />;
}
